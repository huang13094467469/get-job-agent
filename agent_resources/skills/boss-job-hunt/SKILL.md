---
name: boss-job-hunt
description: Boss 直聘逐岗求职的进阶打法与异常处置手册。当页面出现登录失效/验证码/风控弹窗/找不到【立即沟通】按钮/岗位卡片结构异常，或在撰写打招呼话术、判断是否值得投递时使用本 skill 读取详细指引。
---

# Boss 直聘求职 — 进阶打法与异常处置

本 skill 为**按需加载**的深度指引。主流程 SOP 已在长期记忆（AGENTS.md）中始终可用；
此处补充「异常情况怎么办」和「话术怎么写、投递怎么判断」两类需要时才展开的知识。

## 一、异常处置（优先保命，宁可停下也不要乱点）

- **页面未连接 / page_not_connected**：工具返回 `page_not_connected:zhipin` 时，提示用户在
  前台打开 Boss 直聘页面（`https://www.zhipin.com/web/geek/jobs`）并保持登录，不要反复重试。
- **操作超时 page_action_timeout**：页面可能卡顿，先 `browser_act op='wait'` 再 `browser_snapshot`
  重新确认状态；同一操作连续失败两次即停下向用户说明。
- **登录失效 / 需要验证码 / 风控弹窗**：一旦快照里出现「登录」「扫码」「验证码」「安全验证」等，
  **立即停止一切自动操作**，把情况如实告知用户，等待人工处理。绝不尝试填写验证码或模拟登录。
- **ref 漂移点不到目标**：SPA 列表重渲染后 ref 常失效。改用稳健文本定位：省略 `ref`，
  传 `match_text`（目标元素应含的文字，如「立即沟通」）+ `within_text`（所在岗位卡片区分文字，如岗位名）
  + 必要时 `occurrence` 取第 N 个命中。
- **点【立即沟通】没进聊天页**：可能跳到中间页或弹【继续沟通】。`op='wait'` 等页面稳定后重新
  `browser_snapshot`；出现【继续沟通】就 `match_text='继续沟通'` 点进去，直到 URL 含 `/web/geek/chat`。
- **岗位卡片字段缺失**：列表里拿不到职责/技能时不要凭残缺信息投递，点进详情页补全后再
  `compare_job_with_resume`。

## 二、打招呼话术撰写规范

话术经 `send_greeting` 发送，且必须用户确认后才真正发出。撰写要求：

- **短**：2~4 句、控制在 60~120 字，HR 一眼能读完。
- **真诚、不谄媚**：不要「贵公司是最好的」这类空话。
- **突出与该岗位的匹配点**：从 `compare_job_with_resume` 命中的技能/经历里挑 1~2 个最相关的点明。
- **带一个轻量行动召唤**：如「方便的话想进一步沟通，我的简历已发～」，不要施压。
- **结构模板**（示意，勿照抄）：
  1. 问候 + 对岗位的兴趣（点名岗位名/方向）
  2. 一句最硬的匹配点（技能或项目经历，对齐 JD 关键要求）
  3. 一句礼貌的行动召唤
- **禁止**：编造未写在简历里的经历/技能；出现薪资谈判、离职原因等敏感话题（这是后续沟通阶段的事）。

## 三、是否值得投递的判断

- 以 `compare_job_with_resume` 的结论为准：仅「符合，可进入沟通环节」才投递。
- 「匹配偏弱，谨慎沟通」或「岗位字段缺口过大」时：前者可结合职责主观斟酌，后者必须先补全 JD 再判。
- 已沟通过（`contacted=true` / 按钮【继续沟通】/ 带【已沟通】标记）的岗位一律跳过，避免重复打扰。
- 单轮会话有系统级投递上限；达到上限时 `send_greeting` 会被拒绝，此时总结已投递岗位并结束，不要重试。
